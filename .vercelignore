# build sources and dev-only tooling — not served, not needed at runtime
src/
migrate/
api/dev/
devserver.mjs
*.md
promptlibrary-push-package/
prompt-library-vercel.zip

# Next.js strangler migration (prompt-library/web/) — lives in this repo/deploy
# for version control, but is NOT part of the legacy site's build. It has its
# own package.json/next.config and needs its own Vercel project (not yet
# created) before it's served anywhere. Per the approved migration plan: no
# production cutover, LIBRARY_V2/HOME_V2 ship OFF. See MIGRATION.md.
web/
