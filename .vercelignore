# build sources and dev-only tooling — not served, not needed at runtime
src/
migrate/
api/dev/
devserver.mjs
*.md
promptlibrary-push-package/
prompt-library-vercel.zip

# Next.js strangler migration (prompt-library/web/). This file is the LEGACY
# Vercel project's ignore list — the legacy project deploys the repo root
# (index.html + api/**) and must NOT build web/. The strangler runs as a
# SEPARATE Vercel project with Root Directory = "web" (its own web/vercel.json).
# On that project the marketing landing (LANDING_V2) ships ON; LIBRARY_V2 /
# HOME_V2 still ship OFF. See MIGRATION.md §13 "Deploying web/".
web/
